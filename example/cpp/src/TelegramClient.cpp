// Auto-generated Telegram Bot API client for C++
// Generated by telegram-api-generator. Do not edit manually.

#include "TelegramClient.hpp"

namespace tgbot {

TelegramClient::TelegramClient(boost::asio::any_io_executor executor, const std::string& api_key)
    : api_key_(api_key), http_client_(std::move(executor)) {
}

template<typename T>
TelegramResponse<T> TelegramClient::parseResponse(const std::string& response_str) {
    auto j = json::parse(response_str);
    TelegramResponse<T> result;
    result.ok = j["ok"].get<bool>();
    if (result.ok && j.contains("result")) {
        result.result = j["result"].get<T>();
    }
    return result;
}


// Getting updates

boost::asio::awaitable<TelegramResponse<std::vector<Update::Ptr>>> TelegramClient::getUpdates(const tgbot::GetUpdatesRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getUpdates";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::vector<Update::Ptr>>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setWebhook(const tgbot::SetWebhookRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setWebhook";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteWebhook(const tgbot::DeleteWebhookRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteWebhook";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<WebhookInfo::Ptr>> TelegramClient::getWebhookInfo() {
    const std::string target = "/bot" + api_key_ + "/getWebhookInfo";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::get, target);
    co_return parseResponse<WebhookInfo::Ptr>(response);
}


// Available methods

boost::asio::awaitable<TelegramResponse<User::Ptr>> TelegramClient::getMe() {
    const std::string target = "/bot" + api_key_ + "/getMe";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::get, target);
    co_return parseResponse<User::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::logOut() {
    const std::string target = "/bot" + api_key_ + "/logOut";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::get, target);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::close() {
    const std::string target = "/bot" + api_key_ + "/close";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::get, target);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendMessage(const tgbot::SendMessageRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendMessage";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::forwardMessage(const tgbot::ForwardMessageRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/forwardMessage";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<std::vector<MessageId::Ptr>>> TelegramClient::forwardMessages(const tgbot::ForwardMessagesRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/forwardMessages";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::vector<MessageId::Ptr>>(response);
}

boost::asio::awaitable<TelegramResponse<MessageId::Ptr>> TelegramClient::copyMessage(const tgbot::CopyMessageRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/copyMessage";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<MessageId::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<std::vector<MessageId::Ptr>>> TelegramClient::copyMessages(const tgbot::CopyMessagesRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/copyMessages";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::vector<MessageId::Ptr>>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendPhoto(const tgbot::SendPhotoRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendPhoto";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendAudio(const tgbot::SendAudioRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendAudio";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendDocument(const tgbot::SendDocumentRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendDocument";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendVideo(const tgbot::SendVideoRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendVideo";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendAnimation(const tgbot::SendAnimationRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendAnimation";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendVoice(const tgbot::SendVoiceRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendVoice";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendVideoNote(const tgbot::SendVideoNoteRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendVideoNote";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendPaidMedia(const tgbot::SendPaidMediaRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendPaidMedia";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<std::vector<Message::Ptr>>> TelegramClient::sendMediaGroup(const tgbot::SendMediaGroupRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendMediaGroup";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::vector<Message::Ptr>>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendLocation(const tgbot::SendLocationRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendLocation";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendVenue(const tgbot::SendVenueRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendVenue";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendContact(const tgbot::SendContactRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendContact";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendPoll(const tgbot::SendPollRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendPoll";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendChecklist(const tgbot::SendChecklistRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendChecklist";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendDice(const tgbot::SendDiceRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendDice";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::sendMessageDraft(const tgbot::SendMessageDraftRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendMessageDraft";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::sendChatAction(const tgbot::SendChatActionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendChatAction";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setMessageReaction(const tgbot::SetMessageReactionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setMessageReaction";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<UserProfilePhotos::Ptr>> TelegramClient::getUserProfilePhotos(const tgbot::GetUserProfilePhotosRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getUserProfilePhotos";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<UserProfilePhotos::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setUserEmojiStatus(const tgbot::SetUserEmojiStatusRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setUserEmojiStatus";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<File::Ptr>> TelegramClient::getFile(const tgbot::GetFileRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getFile";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<File::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::banChatMember(const tgbot::BanChatMemberRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/banChatMember";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::unbanChatMember(const tgbot::UnbanChatMemberRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/unbanChatMember";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::restrictChatMember(const tgbot::RestrictChatMemberRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/restrictChatMember";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::promoteChatMember(const tgbot::PromoteChatMemberRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/promoteChatMember";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setChatAdministratorCustomTitle(const tgbot::SetChatAdministratorCustomTitleRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setChatAdministratorCustomTitle";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::banChatSenderChat(const tgbot::BanChatSenderChatRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/banChatSenderChat";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::unbanChatSenderChat(const tgbot::UnbanChatSenderChatRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/unbanChatSenderChat";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setChatPermissions(const tgbot::SetChatPermissionsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setChatPermissions";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<std::string>> TelegramClient::exportChatInviteLink(const tgbot::ExportChatInviteLinkRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/exportChatInviteLink";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::string>(response);
}

boost::asio::awaitable<TelegramResponse<ChatInviteLink::Ptr>> TelegramClient::createChatInviteLink(const tgbot::CreateChatInviteLinkRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/createChatInviteLink";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ChatInviteLink::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<ChatInviteLink::Ptr>> TelegramClient::editChatInviteLink(const tgbot::EditChatInviteLinkRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editChatInviteLink";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ChatInviteLink::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<ChatInviteLink::Ptr>> TelegramClient::createChatSubscriptionInviteLink(const tgbot::CreateChatSubscriptionInviteLinkRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/createChatSubscriptionInviteLink";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ChatInviteLink::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<ChatInviteLink::Ptr>> TelegramClient::editChatSubscriptionInviteLink(const tgbot::EditChatSubscriptionInviteLinkRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editChatSubscriptionInviteLink";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ChatInviteLink::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<ChatInviteLink::Ptr>> TelegramClient::revokeChatInviteLink(const tgbot::RevokeChatInviteLinkRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/revokeChatInviteLink";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ChatInviteLink::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::approveChatJoinRequest(const tgbot::ApproveChatJoinRequestRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/approveChatJoinRequest";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::declineChatJoinRequest(const tgbot::DeclineChatJoinRequestRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/declineChatJoinRequest";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setChatPhoto(const tgbot::SetChatPhotoRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setChatPhoto";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteChatPhoto(const tgbot::DeleteChatPhotoRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteChatPhoto";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setChatTitle(const tgbot::SetChatTitleRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setChatTitle";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setChatDescription(const tgbot::SetChatDescriptionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setChatDescription";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::pinChatMessage(const tgbot::PinChatMessageRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/pinChatMessage";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::unpinChatMessage(const tgbot::UnpinChatMessageRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/unpinChatMessage";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::unpinAllChatMessages(const tgbot::UnpinAllChatMessagesRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/unpinAllChatMessages";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::leaveChat(const tgbot::LeaveChatRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/leaveChat";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<ChatFullInfo::Ptr>> TelegramClient::getChat(const tgbot::GetChatRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getChat";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ChatFullInfo::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<std::vector<ChatMember::Ptr>>> TelegramClient::getChatAdministrators(const tgbot::GetChatAdministratorsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getChatAdministrators";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::vector<ChatMember::Ptr>>(response);
}

boost::asio::awaitable<TelegramResponse<int64_t>> TelegramClient::getChatMemberCount(const tgbot::GetChatMemberCountRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getChatMemberCount";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<int64_t>(response);
}

boost::asio::awaitable<TelegramResponse<ChatMember::Ptr>> TelegramClient::getChatMember(const tgbot::GetChatMemberRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getChatMember";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ChatMember::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setChatStickerSet(const tgbot::SetChatStickerSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setChatStickerSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteChatStickerSet(const tgbot::DeleteChatStickerSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteChatStickerSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<std::vector<Sticker::Ptr>>> TelegramClient::getForumTopicIconStickers() {
    const std::string target = "/bot" + api_key_ + "/getForumTopicIconStickers";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::get, target);
    co_return parseResponse<std::vector<Sticker::Ptr>>(response);
}

boost::asio::awaitable<TelegramResponse<ForumTopic::Ptr>> TelegramClient::createForumTopic(const tgbot::CreateForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/createForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ForumTopic::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::editForumTopic(const tgbot::EditForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::closeForumTopic(const tgbot::CloseForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/closeForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::reopenForumTopic(const tgbot::ReopenForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/reopenForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteForumTopic(const tgbot::DeleteForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::unpinAllForumTopicMessages(const tgbot::UnpinAllForumTopicMessagesRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/unpinAllForumTopicMessages";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::editGeneralForumTopic(const tgbot::EditGeneralForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editGeneralForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::closeGeneralForumTopic(const tgbot::CloseGeneralForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/closeGeneralForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::reopenGeneralForumTopic(const tgbot::ReopenGeneralForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/reopenGeneralForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::hideGeneralForumTopic(const tgbot::HideGeneralForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/hideGeneralForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::unhideGeneralForumTopic(const tgbot::UnhideGeneralForumTopicRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/unhideGeneralForumTopic";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::unpinAllGeneralForumTopicMessages(const tgbot::UnpinAllGeneralForumTopicMessagesRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/unpinAllGeneralForumTopicMessages";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::answerCallbackQuery(const tgbot::AnswerCallbackQueryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/answerCallbackQuery";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<UserChatBoosts::Ptr>> TelegramClient::getUserChatBoosts(const tgbot::GetUserChatBoostsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getUserChatBoosts";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<UserChatBoosts::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<BusinessConnection::Ptr>> TelegramClient::getBusinessConnection(const tgbot::GetBusinessConnectionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getBusinessConnection";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<BusinessConnection::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setMyCommands(const tgbot::SetMyCommandsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setMyCommands";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteMyCommands(const tgbot::DeleteMyCommandsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteMyCommands";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<std::vector<BotCommand::Ptr>>> TelegramClient::getMyCommands(const tgbot::GetMyCommandsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getMyCommands";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::vector<BotCommand::Ptr>>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setMyName(const tgbot::SetMyNameRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setMyName";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<BotName::Ptr>> TelegramClient::getMyName(const tgbot::GetMyNameRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getMyName";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<BotName::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setMyDescription(const tgbot::SetMyDescriptionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setMyDescription";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<BotDescription::Ptr>> TelegramClient::getMyDescription(const tgbot::GetMyDescriptionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getMyDescription";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<BotDescription::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setMyShortDescription(const tgbot::SetMyShortDescriptionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setMyShortDescription";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<BotShortDescription::Ptr>> TelegramClient::getMyShortDescription(const tgbot::GetMyShortDescriptionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getMyShortDescription";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<BotShortDescription::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setChatMenuButton(const tgbot::SetChatMenuButtonRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setChatMenuButton";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<MenuButton::Ptr>> TelegramClient::getChatMenuButton(const tgbot::GetChatMenuButtonRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getChatMenuButton";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<MenuButton::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setMyDefaultAdministratorRights(const tgbot::SetMyDefaultAdministratorRightsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setMyDefaultAdministratorRights";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<ChatAdministratorRights::Ptr>> TelegramClient::getMyDefaultAdministratorRights(const tgbot::GetMyDefaultAdministratorRightsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getMyDefaultAdministratorRights";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<ChatAdministratorRights::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::sendGift(const tgbot::SendGiftRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendGift";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::giftPremiumSubscription(const tgbot::GiftPremiumSubscriptionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/giftPremiumSubscription";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::verifyUser(const tgbot::VerifyUserRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/verifyUser";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::verifyChat(const tgbot::VerifyChatRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/verifyChat";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::removeUserVerification(const tgbot::RemoveUserVerificationRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/removeUserVerification";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::removeChatVerification(const tgbot::RemoveChatVerificationRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/removeChatVerification";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::readBusinessMessage(const tgbot::ReadBusinessMessageRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/readBusinessMessage";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteBusinessMessages(const tgbot::DeleteBusinessMessagesRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteBusinessMessages";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setBusinessAccountName(const tgbot::SetBusinessAccountNameRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setBusinessAccountName";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setBusinessAccountUsername(const tgbot::SetBusinessAccountUsernameRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setBusinessAccountUsername";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setBusinessAccountBio(const tgbot::SetBusinessAccountBioRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setBusinessAccountBio";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setBusinessAccountProfilePhoto(const tgbot::SetBusinessAccountProfilePhotoRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setBusinessAccountProfilePhoto";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::removeBusinessAccountProfilePhoto(const tgbot::RemoveBusinessAccountProfilePhotoRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/removeBusinessAccountProfilePhoto";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setBusinessAccountGiftSettings(const tgbot::SetBusinessAccountGiftSettingsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setBusinessAccountGiftSettings";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<StarAmount::Ptr>> TelegramClient::getBusinessAccountStarBalance(const tgbot::GetBusinessAccountStarBalanceRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getBusinessAccountStarBalance";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<StarAmount::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::transferBusinessAccountStars(const tgbot::TransferBusinessAccountStarsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/transferBusinessAccountStars";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<OwnedGifts::Ptr>> TelegramClient::getBusinessAccountGifts(const tgbot::GetBusinessAccountGiftsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getBusinessAccountGifts";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<OwnedGifts::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<OwnedGifts::Ptr>> TelegramClient::getUserGifts(const tgbot::GetUserGiftsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getUserGifts";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<OwnedGifts::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<OwnedGifts::Ptr>> TelegramClient::getChatGifts(const tgbot::GetChatGiftsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getChatGifts";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<OwnedGifts::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::convertGiftToStars(const tgbot::ConvertGiftToStarsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/convertGiftToStars";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::upgradeGift(const tgbot::UpgradeGiftRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/upgradeGift";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::transferGift(const tgbot::TransferGiftRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/transferGift";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<Story::Ptr>> TelegramClient::postStory(const tgbot::PostStoryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/postStory";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Story::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Story::Ptr>> TelegramClient::repostStory(const tgbot::RepostStoryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/repostStory";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Story::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Story::Ptr>> TelegramClient::editStory(const tgbot::EditStoryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editStory";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Story::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteStory(const tgbot::DeleteStoryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteStory";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}


// Updating messages

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::editMessageText(const tgbot::EditMessageTextRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editMessageText";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::editMessageCaption(const tgbot::EditMessageCaptionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editMessageCaption";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::editMessageMedia(const tgbot::EditMessageMediaRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editMessageMedia";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::editMessageLiveLocation(const tgbot::EditMessageLiveLocationRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editMessageLiveLocation";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::stopMessageLiveLocation(const tgbot::StopMessageLiveLocationRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/stopMessageLiveLocation";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::editMessageChecklist(const tgbot::EditMessageChecklistRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editMessageChecklist";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::editMessageReplyMarkup(const tgbot::EditMessageReplyMarkupRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editMessageReplyMarkup";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Poll::Ptr>> TelegramClient::stopPoll(const tgbot::StopPollRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/stopPoll";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Poll::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::approveSuggestedPost(const tgbot::ApproveSuggestedPostRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/approveSuggestedPost";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::declineSuggestedPost(const tgbot::DeclineSuggestedPostRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/declineSuggestedPost";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteMessage(const tgbot::DeleteMessageRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteMessage";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteMessages(const tgbot::DeleteMessagesRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteMessages";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}


// Stickers

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendSticker(const tgbot::SendStickerRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendSticker";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<StickerSet::Ptr>> TelegramClient::getStickerSet(const tgbot::GetStickerSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getStickerSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<StickerSet::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<std::vector<Sticker::Ptr>>> TelegramClient::getCustomEmojiStickers(const tgbot::GetCustomEmojiStickersRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getCustomEmojiStickers";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::vector<Sticker::Ptr>>(response);
}

boost::asio::awaitable<TelegramResponse<File::Ptr>> TelegramClient::uploadStickerFile(const tgbot::UploadStickerFileRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/uploadStickerFile";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<File::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::createNewStickerSet(const tgbot::CreateNewStickerSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/createNewStickerSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::addStickerToSet(const tgbot::AddStickerToSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/addStickerToSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setStickerPositionInSet(const tgbot::SetStickerPositionInSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setStickerPositionInSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteStickerFromSet(const tgbot::DeleteStickerFromSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteStickerFromSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::replaceStickerInSet(const tgbot::ReplaceStickerInSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/replaceStickerInSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setStickerEmojiList(const tgbot::SetStickerEmojiListRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setStickerEmojiList";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setStickerKeywords(const tgbot::SetStickerKeywordsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setStickerKeywords";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setStickerMaskPosition(const tgbot::SetStickerMaskPositionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setStickerMaskPosition";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setStickerSetTitle(const tgbot::SetStickerSetTitleRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setStickerSetTitle";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setStickerSetThumbnail(const tgbot::SetStickerSetThumbnailRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setStickerSetThumbnail";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setCustomEmojiStickerSetThumbnail(const tgbot::SetCustomEmojiStickerSetThumbnailRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setCustomEmojiStickerSetThumbnail";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::deleteStickerSet(const tgbot::DeleteStickerSetRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/deleteStickerSet";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}


// Inline mode

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::answerInlineQuery(const tgbot::AnswerInlineQueryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/answerInlineQuery";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<SentWebAppMessage::Ptr>> TelegramClient::answerWebAppQuery(const tgbot::AnswerWebAppQueryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/answerWebAppQuery";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<SentWebAppMessage::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<PreparedInlineMessage::Ptr>> TelegramClient::savePreparedInlineMessage(const tgbot::SavePreparedInlineMessageRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/savePreparedInlineMessage";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<PreparedInlineMessage::Ptr>(response);
}


// Payments

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendInvoice(const tgbot::SendInvoiceRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendInvoice";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<std::string>> TelegramClient::createInvoiceLink(const tgbot::CreateInvoiceLinkRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/createInvoiceLink";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::string>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::answerShippingQuery(const tgbot::AnswerShippingQueryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/answerShippingQuery";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::answerPreCheckoutQuery(const tgbot::AnswerPreCheckoutQueryRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/answerPreCheckoutQuery";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<StarTransactions::Ptr>> TelegramClient::getStarTransactions(const tgbot::GetStarTransactionsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getStarTransactions";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<StarTransactions::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::refundStarPayment(const tgbot::RefundStarPaymentRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/refundStarPayment";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::editUserStarSubscription(const tgbot::EditUserStarSubscriptionRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/editUserStarSubscription";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}


// Telegram Passport

boost::asio::awaitable<TelegramResponse<bool>> TelegramClient::setPassportDataErrors(const tgbot::SetPassportDataErrorsRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setPassportDataErrors";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<bool>(response);
}


// Games

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::sendGame(const tgbot::SendGameRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/sendGame";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<Message::Ptr>> TelegramClient::setGameScore(const tgbot::SetGameScoreRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/setGameScore";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<Message::Ptr>(response);
}

boost::asio::awaitable<TelegramResponse<std::vector<GameHighScore::Ptr>>> TelegramClient::getGameHighScores(const tgbot::GetGameHighScoresRequest& request) {
    json j = request;
    std::string body = j.dump();
    const std::string target = "/bot" + api_key_ + "/getGameHighScores";
    std::string response = co_await http_client_.makeRequest(boost::beast::http::verb::post, target, body);
    co_return parseResponse<std::vector<GameHighScore::Ptr>>(response);
}

} // namespace tgbot
