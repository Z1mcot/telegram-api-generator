// Auto-generated Telegram Bot API models for C++
// Generated by telegram-api-generator. Do not edit manually.

#include "requests/SendMessageDraftRequest.hpp"
#include "types/MessageEntity.hpp"
#include <nlohmann/json.hpp>

namespace tgbot {
    void to_json(json& j, const SendMessageDraftRequest& value) {
        j = json{
            {"chat_id", value.chat_id},
            {"draft_id", value.draft_id},
            {"text", value.text},
            {"message_thread_id", value.message_thread_id},
            {"parse_mode", value.parse_mode},
            {"entities", value.entities}
        };
    }

    void from_json(const json& j, SendMessageDraftRequest& value) {
        if (j.contains("chat_id")) {
            j.at("chat_id").get_to(value.chat_id);
        }
        if (j.contains("draft_id")) {
            j.at("draft_id").get_to(value.draft_id);
        }
        if (j.contains("text")) {
            j.at("text").get_to(value.text);
        }
        if (j.contains("message_thread_id")) {
            j.at("message_thread_id").get_to(value.message_thread_id);
        }
        if (j.contains("parse_mode")) {
            j.at("parse_mode").get_to(value.parse_mode);
        }
        if (j.contains("entities")) {
            j.at("entities").get_to(value.entities);
        }
    }
}
